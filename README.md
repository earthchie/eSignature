# eSignature for PDF on client side

This repo should be ready to use out of the box.
However, if you wish to re-build node module, then:

```javascript
npm install
browserify include.js -o pkiwebsdk.js
```

Cheers!
